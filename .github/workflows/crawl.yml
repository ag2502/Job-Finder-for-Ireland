name: Crawl

# Fetching only. This workflow does one thing — pull current openings and reconcile them
# — because that is the only part of the pipeline whose underlying data changes hourly.
#
# Growing the registry (import, detection, extraction) lives in `registry.yml` on a
# weekly schedule. Splitting them matters: detection takes 30-60 minutes across a few
# hundred unknown hosts and almost never finds anything new, so running it four times a
# day spent most of the CI budget re-confirming what was already known and pushed this
# job toward its timeout for no benefit.
#
# The two workflows never call each other. `registry.yml` registers sources with
# `last_success_at = NULL`, which `select_sources` treats as always due, so the next
# crawl picks them up on its own.

on:
  schedule:
    # Roughly every six hours. GitHub queues scheduled workflows with everyone else's,
    # so expect drift of minutes to tens of minutes; nothing here depends on exact times.
    - cron: "0 */6 * * *"
  workflow_dispatch:
    inputs:
      adapter:
        description: "Restrict the run to one adapter (optional)"
        required: false
      all_sources:
        description: "Ignore the staleness schedule and crawl everything"
        type: boolean
        default: false

permissions:
  # Publishing the crawler's database as a Release asset needs write access to contents.
  contents: write

concurrency:
  # Shared with registry.yml. Both write to the same database, and the reconciler's
  # guarantees assume one writer: two runs interleaving could close jobs that the other
  # run had just seen as open.
  group: jobfinder-db
  cancel-in-progress: false

jobs:
  crawl:
    runs-on: ubuntu-latest
    # Steady-state runs take 5-10 minutes because the staleness rule skips most sources.
    # The headroom is for a cold start, where every source is due at once.
    timeout-minutes: 60

    env:
      JOBFINDER_LOG_LEVEL: INFO
      # The crawler's database is a file in the workspace, pulled from and pushed back to
      # a Release by scripts/state_db.sh. Steps that touch it name this URL; the test step
      # deliberately does not, so it falls through to the committed snapshot.
      STATE_DB_URL: sqlite:///${{ github.workspace }}/jobfinder.db
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      # JOBFINDER_DATABASE_URL is deliberately NOT set job-wide. It used to be, and that
      # meant `pytest` ran the web tests against the production database: when Neon hit
      # its data-transfer quota the tests could not connect, the run failed, and the
      # crawl never executed — a website problem silently stopped the crawler. The steps
      # that genuinely write to production now opt in individually, and the tests fall
      # through to the committed snapshot instead.

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run tests
        # A broken reconciler must never reach the production database: a bug here could
        # close jobs that are still open, and that is the one failure this project exists
        # to prevent. Three minutes is cheap insurance against an unrecoverable mistake.
        run: pytest -q

      - name: Restore the crawler state
        run: bash scripts/state_db.sh pull

      - name: Crawl
        env:
          JOBFINDER_DATABASE_URL: ${{ env.STATE_DB_URL }}
        run: |
          args=""
          if [ -n "${{ github.event.inputs.adapter }}" ]; then
            args="$args --adapter ${{ github.event.inputs.adapter }}"
          fi
          if [ "${{ github.event.inputs.all_sources }}" = "true" ]; then
            args="$args --all"
          fi
          jobfinder crawl $args

      - name: Publish the crawler state
        # Only on success. A run that failed part-way through has a database that reflects
        # neither the previous state nor a complete crawl, and overwriting the release with
        # it would make the damage permanent — the reconciler would treat the missing jobs
        # as absences and start closing them.
        run: bash scripts/state_db.sh push

      - name: Refresh the snapshot the website serves
        env:
          JOBFINDER_DATABASE_URL: ${{ env.STATE_DB_URL }}
        # The site reads a bundled file, never the database, so a crawl does not reach
        # visitors until this rebuilds it and the next step ships it.
        run: python scripts/export_snapshot.py

      - name: Keep the accounts project awake
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
        # Supabase pauses a free project after about a week without traffic, and the
        # first request after that fails - which for a job site means someone cannot
        # sign in at the exact moment they came back to look. This crawl already runs
        # every six hours, so one request from it is enough to keep the project awake
        # and costs nothing.
        #
        # Never fails the crawl. The jobs are what this workflow is for; sign-in being
        # briefly unavailable is not a reason to skip publishing a day's postings.
        if: ${{ env.SUPABASE_URL != '' }}
        continue-on-error: true
        run: |
          code=$(curl -s -o /dev/null -w '%{http_code}' --max-time 20 \
            -H "apikey: $SUPABASE_ANON_KEY" \
            "$SUPABASE_URL/rest/v1/applications?select=id&limit=1" || echo 000)
          echo "Supabase responded $code"

      - name: Deploy the refreshed snapshot
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          # Read automatically by the Vercel CLI. They have to be supplied because
          # `.vercel/project.json`, where a local checkout keeps them, is not committed.
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        # The site serves the snapshot baked into its bundle, so a crawl reaches visitors
        # only when a new deployment carries the rebuilt file. The project is not
        # connected to this repository — a push deploys nothing — so the deployment is
        # made here rather than left to a Git trigger that does not exist.
        #
        # The snapshot is deliberately NOT committed. It is a 12 MB binary that changes
        # completely every run, so committing it four times a day would add roughly
        # 50 MB of pack objects daily to a repository that gains nothing from the
        # history: only the newest snapshot is ever served.
        run: |
          if [ -z "$VERCEL_TOKEN" ]; then
            echo "::error::VERCEL_TOKEN is not set. Create one at"
            echo "::error::https://vercel.com/account/tokens and add it under"
            echo "::error::Settings > Secrets and variables > Actions."
            exit 1
          fi
          npx --yes vercel@latest deploy --prod --yes --token="$VERCEL_TOKEN"

      - name: Summary
        env:
          JOBFINDER_DATABASE_URL: ${{ env.STATE_DB_URL }}
        if: always()
        run: |
          {
            echo "## Crawl"
            echo '```'
            jobfinder stats
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
